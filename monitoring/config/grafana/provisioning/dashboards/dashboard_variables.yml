apiVersion: 1

variables:
  - name: DS_PROMETHEUS
    type: datasource
    datasource: vm_ds
    
  - name: DS_DS_PROMETHEUS
    type: datasource
    datasource: vm_ds
    
  - name: DS_LOCALHOST
    type: datasource
    datasource: vm_ds
