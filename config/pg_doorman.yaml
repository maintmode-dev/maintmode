# pg_doorman configuration
# Connection pooler for PostgreSQL

pools:
  maintmode:
    server_host: postgres
    server_port: 5432
    database: maintmode
    pool_mode: transaction
    pool_size: 40
    users:
      postgres:
        password: postgres

# Prometheus metrics
metrics:
  listen_addr: ":9127"
